Action Methods
These are methods in C# Controller class. They provide response to any particular URL in the web.
They are public.
They are non-static.

IActionResult
It's an interface of teh ActionResult abstract class, used to implement ViewResult, EmptyResult, StatusCodeResult, PartialViewResult etc.